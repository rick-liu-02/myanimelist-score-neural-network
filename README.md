# myanimelist-score-neural-network
A program that uses a neural network to try and predict the user ratings, ranging from zero to ten, of MyAnimeList entries. ~0.9 mean absolute error on test datasets. Requires the modules TensorFlow and JikanPy.
